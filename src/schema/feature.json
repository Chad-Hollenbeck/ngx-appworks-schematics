{
  "$id": "firestore-component-schema",
  "$schema": "http://json-schema.org/schema",
  "properties": {
    "featureName": {
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "description": "The name of the new feature to be created",
      "type": "string",
      "x-prompt": "What is the name for your new feature?"
    }
  },
  "required": [
    "featureName"
  ],
  "title": "Feature Schema",
  "type": "object"
}
