{
  "$schema": "http://json-schema.org/schema",
  "id": "firestore-service-schema",
  "title": "Firestore Service Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "className": {
      "type": "string",
      "description" : "What class should be used for typings?",
      "x-prompt" : "What class are you using for this firestore crud service? Leave 'VM' off of the class name."
    }
  },
  "required": [
    "name",
    "className"
  ]
}
